# Model Approval

Registration does not equal approval. The local registry supports `approve`, `approve_with_conditions`, `reject`, and `defer` decisions. Decisions require an actor, reason, timestamp, evidence fingerprint, and prior status.

The committed candidate remains unapproved. The demonstration approval state is pending/deferred for governance review because locked test specificity is weak and all evidence is synthetic.
