# Release Versioning

Release versioning uses the project version from `pyproject.toml` and a Git revision. Because the repository currently has no commits, local evidence uses `uncommitted` rather than inventing a Git SHA.

Image tags follow semantic version plus revision, for example `0.1.0-uncommitted` before the first commit. Images are not pushed to a registry in Milestone 13.
