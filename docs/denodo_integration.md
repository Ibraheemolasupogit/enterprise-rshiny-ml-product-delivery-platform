# Denodo Integration

## Intended Role

Denodo may later provide a governed virtualisation layer over synthetic source systems. It would support reusable logical views, lineage, and controlled access patterns.

## Genuine Integration Path

A future `real_denodo` implementation must use real connection configuration, tested queries, honest evidence, and documentation showing how views are created and validated. No fabricated screenshots or claims are permitted.

## Local Fallback

The project must remain runnable without Denodo. A SQL-compatible local governed logical-view fallback may be labelled `denodo_compatible_local`.

## Evidence Requirements

Evidence must include configuration mode, connection boundaries, validated view definitions, and limitations. Milestone 1 contains documentation and directories only.

## Current Status

Real Denodo integration: externally blocked and not implemented.

Reason: genuine access unavailable.

Fake evidence created: no.

Local compatible logical layer: implemented using DuckDB.

Provider label: `denodo_compatible_local`.

The local layer must not be described as Denodo evidence.
